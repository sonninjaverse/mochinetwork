import DefaultTheme from "vitepress/theme";
import "./custom.css";

/**
 * The default theme, recoloured to the app's tokens. VitePress exposes its
 * palette as CSS variables, so matching the app is a variable swap rather than
 * a theme of its own.
 */
export default DefaultTheme;
